# Ancient reward artwork

`thethirdmovementcard.png`, `charismaticformcard.png`, and `enchantedhairband.png` retain the user-supplied PNGs without repainting or cropping. The relic source was supplied as `enchantedblazinghairband.png`. Packaged portraits and relic icons are direct copies. The relic's alpha channel exactly matches the existing Blazing Hairband image, allowing its existing white silhouette to be reused as the outline.

`charismaticformcard-previous.png` and `charismaticformcard-previous-big.png` preserve the previous packaged portraits. This source folder is not selected for PCK export.
